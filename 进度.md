# anydoc C ABI 封装 —— 进度

> 依据《方案_c_abi_anydoc.md》路线 C，采用决策 3.1 选项 A（只导 Markdown）+ 决策 3.2 Caller-free + 决策 3.3 int 错误码。

## 总览

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P0 | `src/capi.rs` + `cbindgen.toml` + 生成 `anydoc.h` | ✅ 已完成 |
| P1 | 三平台编译出 `.dylib/.so/.dll` | 🟡 mac 已出 .dylib；linux/win 待交叉编译 |
| P2 | C++ 加载层 `AnyDocBackend.h/.cpp` | ✅ 已完成 |
| P3 | 集成进 app | ⬜ 未开始 |
| P4 | 打包与分发 | ⬜ 未开始 |
| P5 | Document→JSON（可选） | ⬜ 未开始 |

## P0 明细（已完成）

- [x] `src/capi.rs`：导出 `extern "C"` 函数
- [x] `cbindgen.toml`
- [x] `src/lib.rs` 注册模块
- [x] 生成 `include/anydoc.h`
- [x] `cargo build --release` 验证符号
- [x] 最小 C 探针端到端验证（dlopen + 调用 + 释放 + 错误路径）

## 导出函数清单（已实现）

| 函数 | 说明 |
| --- | --- |
| `anydoc_to_markdown(path)` | 路径转 Markdown，返回 `char*`（NULL=失败） |
| `anydoc_to_markdown_bytes(data, len, fmt)` | 内存字节转 Markdown，`fmt=-1` 自动识别 |
| `anydoc_format_from_bytes(data, len)` | 内容识别格式，返回 `int` |
| `anydoc_format_from_extension(ext)` | 扩展名识别格式 |
| `anydoc_format_from_path(path)` | 路径识别格式 |
| `anydoc_string_free(ptr)` | 释放返回的字符串 |
| `anydoc_last_error()` | 最近错误消息（`char*`，NULL=无错误） |
| `anydoc_error_code()` | 最近错误码（`int`，0=无错误） |

## 错误码约定

`AnyDocErrorCode`（`include/anydoc.h`）：

| 值 | 名称 | 对应 `ConvertError::code()` |
| --- | --- | --- |
| 0 | `Ok` | 成功 |
| 1 | `Unsupported` | `unsupported` |
| 2 | `Malformed` | `malformed` |
| 3 | `Encrypted` | `encrypted` |
| 4 | `ResourceLimit` | `resourceLimit` |
| 5 | `MissingPart` | `missingPart` |
| 6 | `Io` | `io` |

## 格式码约定

`AnyDocFormat`（`include/anydoc.h`）：`Unknown=-1`，`Doc=0`…`Csv=11`（共 12 种格式）。

## 编译产物

- `target/release/libanydoc.dylib`（mac 动态库）
- `target/release/libanydoc.rlib`（静态库，node/python 依赖）
- 主 crate 已加 `crate-type = ["rlib", "cdylib"]`

> 注：动态库名为 `libanydoc.dylib`（主 crate 名），而非方案里的 `libanydoc_capi.dylib`。
> 若需改名，可新建独立 `capi/` 子 crate（仿 node/python）产出 `libanydoc_capi.*`。

## 技术要点 / 踩坑记录

1. **工具链**：本机 `/usr/local/bin/cargo` 是旧版 1.80.1，需用 rustup 管理的新工具链：
   ```
   export PATH="/Users/fans/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH"
   export RUSTC="/Users/fans/.rustup/toolchains/stable-aarch64-apple-darwin/bin/rustc"
   ```
   cbindgen 需 nightly 工具链（`-Zunpretty=expanded`）。

2. **cbindgen 常量冲突**：crate 内存在多个同名 `pub const`（`LEVELS`×3、`LIST_LEVELS`×2），
   导致 cbindgen 解析冲突。已将相关内部常量改为 `pub(crate)`（`docx/numbering`、`pptx/cascade`、
   `rtf/tables`、`odf/styles`、`doc/lists`、`package/limits`），零外部影响。

3. **头文件裁剪**：`[export] item_types = ["enums", "functions"]` 过滤掉无关的
   `Style` 等 struct；`usize_is_size_t = true` 让 `len` 参数映射为 `size_t`。

4. **cbindgen 枚举污染**：`[enum] prefix_with_name = true` 让枚举成员带前缀
   （`AnyDocErrorCode_Ok` / `AnyDocFormat_Doc`），避免裸 `Ok`/`Io`/`Unknown`
   污染 C++ 全局命名空间。

## P2 明细（已完成）

> C++ 加载层（`AnyDocBackend`）实现 dlopen/LoadLibrary 封装、函数指针解析、门面与错误映射，
> 可独立集成到任意 C++ 项目。

- [x] `AnyDocBackend.h`：`docmarkdown` 门面类（单例）+ `Result` + `Error` 枚举
- [x] `AnyDocBackend.cpp`：跨平台 `dlopen`/`LoadLibrary` + 函数指针解析 + 门面 + 错误映射
- [x] `anydoc.h`：本仓库生成的 C ABI 头文件（作为权威声明）

**核心设计**：
- 懒加载单例，多候选库路径（可执行文件同目录 → mac bundle Frameworks → cwd `./` → 安装目录 → 裸名）
- 同时尝试 `libanydoc_capi.*` 与 `libanydoc.*` 两种命名（兼容独立子 crate 与主 crate 内联两种布局）
- Caller-free：`ScopedString` RAII 析构调 `anydoc_string_free`
- 错误码 → `Error` 枚举（含后端级 `LibraryNotFound`/`SymbolMissing`），消息经 `anydoc_last_error` 透传
- 缺库优雅降级：`available()==false` + `unavailable_reason()`

**端到端验证（macOS arm64）**：`dlopen` → `to_markdown(text.docx)` 返回 1316 字节 GFM；
格式识别、错误路径（`io(6)`）、内存释放全链路正确。

## 下一步

- [ ] P1：linux/win 交叉编译验证 `.so`/`.dll`
- [ ] P3：集成进 app，新建文档预览插件，`load()` 调 `AnyDocBackend::to_markdown` 复用文本渲染链路
- [ ] P4：动态库随 app 分发（CMake 集成、bundle 布局）
- [ ] 可选：生成头文件步骤集成进 `scripts/` 或构建脚本

## 日志

- 2026-08-16：完成 P0，端到端探针验证通过。
- 2026-08-16：完成 P2（C++ 加载层），macOS 端到端验证通过。
